/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/mindcrypto',
  images: {
    unoptimized: true,
  },
  assetPrefix: '/mindcrypto/',
};

export default nextConfig;

 